---
# FreeBSD vars for vbotka.doctl

# TODO: Check Linux Binary Compatibility configured

doctl_packages:
  - "devel/py-pip"

doctl_bin_dir: "/usr/local/bin"
doctl_bin_dir_mode: "0755"
doctl_bin_owner: "root"
doctl_bin_group: "wheel"
doctl_bin_mode: "0755"

doctl_pip_executable: "/usr/local/bin/pip-3.6"

# EOF
...
